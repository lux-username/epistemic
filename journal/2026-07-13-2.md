# 2026-07-13 — Session 6: crux-recall fix (analyze first test, act on the one real finding)

Reviewed Session 5's omega-3 test results and asked the load-bearing question: we tied base Claude on **crux-finding**, and that's the product's whole pitch — should we accept "rigor, not gestalt," or fix it? Concluded the "tie" was partly a scoring artifact and partly one real, diagnosable deficit. Fixed the deficit; deferred validation to the hard aspartame strip.

## What was done

**Re-analysis (substance, not scorecard).** Read both runs' actual crux cards. The rubric scored crux-finding by *count* (A′ 6, B 3) — wrong metric for a selectivity tool. On substance: B's crux *reasoning* was as-good-or-cleaner (it treated near-settled OTC-dose as background; A′ inflated it into a live crux). B's only genuine failure was a **recall** miss — it never generated the Omega-3-Index / replacement-vs-enhancement camp. Not a mis-routing; the candidate was never created.

**Pipeline audit.** Checked all 7 skills for a *recall* counterweight to their *pruning/humility* discipline. Verdict: `operationalize` (enumerate framings + carry alternatives) and `write-update-rules` (enumerate mutually-exhaustive outcomes) already have one; `appraise-study` isn't a coverage stage. The all-prune/no-recall failure is concentrated at the **lit-review → find-cruxes seam**. `find-pending-evidence` and the orchestrator flagged minor (honest disclosure, but thin/absent active recall).

**Four edits (design-of-record).**
- lit-review §1: **coverage sweep** for camps a hierarchy search misses; §7: carry minority camps forward with evidence status.
- find-cruxes: **completeness sweep as step-1 third source, before pruning** (single-gate — chosen over a post-pruning append precisely because an appended crux would bypass the honesty valve); **"one gate only"** invariant; **"Considered, not cruxes"** legibility line.
- comparison-rubric (un-versioned, `../epistemic_tests/`): score crux **quality + completeness, not count**.

Design bet: recall at the front, honesty valve at the single exit → out-surface base Claude without endangering Run B's honesty pass. Unvalidated until the aspartame strip.

## Decisions (rationale in decisions.md)
- Act on the first test's crux-finding finding now (don't wait for a second run) — the mechanism was diagnosable from reading the skill, not inferred from n=1. **Reverses** Session 5's "no skill fix warranted from A′."
- Completeness pass placed *before* pruning, not after — so recalled candidates pass the same gate; a post-pruning append would be the exact honesty-floor risk.
- Left `find-pending-evidence` / orchestrator unedited (honest as-is; recall there is enhancement, not correctness).

## Issues
- **Opened:** #25–#31 — one wording-review issue per pipeline skill (each carries its own audit verdict; #26 lit-review and #27 find-cruxes are the two actually edited tonight; #28/#31 note minor recall gaps; #30 cross-refs #23).
- No issue for the aspartame validation — already covered by #18/#19.

## Transcript
See `sessions/2026-07-13-2.transcript.md`.
