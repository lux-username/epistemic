# 2026-07-13 · Session 7 — de-egg (#22) + generality sweep of the pipeline skills

Transcript: `sessions/2026-07-13-3.transcript.md`

## What was done

Executed **#22 (de-egg)** and let it widen into a full generality pass on the seven pipeline skills.

- **Inventoried every egg reference** and split it two ways: the skills' *illustrative* worked-example thread (the target of #22) vs. records that legitimately keep "eggs" (STATUS/decisions/spec/journal — history and the literal competition case). Proposed two non-egg replacement topics (vitamin D, coffee) with mapped previews and let the user choose.
- **Moved the whole worked-example thread to vitamin D → fracture/CVD events** across all 7 skills (operationalize axis table + output contract + description; lit-review current answer; find-cruxes C1 + gaps; find-pending-evidence watch-list + relevance-gate illustration; write-update-rules; crux-watchlist frontmatter slug `vitamin-d-events` + full assembled artifact; plus the estimand/offer illustrations). `appraise-study`'s example was already topic-neutral — only its footer changed.
- **Generality sweep**, prompted by user pushback that the skills must be general:
  - footers `…the real eggs example is #17` → `Values above are illustrative, not a worked case.` (removed showcase + issue-tracker leak);
  - issue-number cross-refs `#7`–`#10` → skill names;
  - in-doc TODO citing `#13` → deleted (verified #13 is OPEN and already captures the cross-cutting runtime-detection work — nothing to re-file);
  - `spec.md` pointer removed; `nutritional-epidemiology` → `observational`;
  - stale `provisional enum` / `finalizes the schema's placeholder` scaffolding → deleted (enum is settled, identical in both docs).
- **Overrode the STATUS gate** ("don't de-egg until the fix is validated") as a deliberate scope call — de-egging illustrative examples is orthogonal to the recall fix. Showcase (#17) left gated.

Verified clean by repeated greps: no `egg` / `#17` / issue-number / `spec.md` / project-jargon references remain in the seven pipeline skills.

## Decisions (full rationale in decisions.md)

- One coherent replacement thread (vitamin D), not a per-skill topic scatter — the pipeline example must chain.
- Chose vitamin D over coffee (exercises the RCT/estimand machinery hardest; 1:1 structural twin of the egg crux).
- Overrode the de-egg gate; kept the showcase half of it intact.
- Standing rule: pipeline skills carry zero project-internal references.

## Issues

- **Closed:** #22 (de-egg) — via commit `closes #22`.
- **Opened:** none. Generality-sweep items were all fixed in-session; the one TODO removed was already tracked in open #13.

## State / next

The recall fix (Session 6) is still **unvalidated** — unchanged. Next run remains the **hard aspartame strip** against the revised quality+completeness rubric; the showcase (#17) waits on it. Test records still outside git (#24); Scite still disconnected.
