# 2026-07-12 (session 4) — Build the functional core

Transcript: `sessions/2026-07-12-4.transcript.md`

## What was done

Built the **entire functional core** of the crux-watchlist skillset, in dependency order, each as a self-contained web-search-floor skill under `.claude/skills/` with its own method, refusal condition, and Sources-&-methods block:

- **#12** artifact schema — home = the orchestrator skill (self-containment); crux-centric body, minimal frontmatter, uniform `unknown`/`insufficient-evidence` convention.
- **#6** `operationalize` (step 0) — four framing axes, estimand declaration (ITT/PP), never-gate rule.
- **#8** `appraise-study` (RoB-2 component) — own signaling questions + own deterministic aggregation; `No Information` → gaps; non-RCT gate. The meatiest piece.
- **#7** `lit-review` (step 1) — discovery, provenance QA (SemanticCite 4-class), GRADE-based body certainty; resolved the appraisal-sequencing tension (grade root claim here, per-crux grading deferred to step 2).
- **#9** `find-cruxes` (step 2) — qualitative VOI as leverage × liveness; per-crux appraisal lands here.
- **#10** `find-pending-evidence` (step 3) — registry-lookup role + runtime tool detection; relevance gate; never fabricate IDs.
- **#11** `write-update-rules` (step 4) — pre-registered conditional updates, mandatory inconclusive branch, qualitative magnitude, data-driven recheck.
- **#5** `crux-watchlist` orchestrator — filled the stub; **paper-traced the full contract chain** (clean); encoded three conductor-only duties (estimand threading, gaps aggregation, degrade-don't-crash).

Then hardened the **test design** (pre-registration, on #19): predicted 5 failure modes + 2 methodological cautions; after user pushback, revised to a sharper 3-run design (framing-matched control / web-search-only *honesty* run / tooled scalability run) and moved validation off eggs. Filed **#22** to de-egg the skills' examples before the eggs showcase.

Reconciled schema enums as each owning skill was built (grade→#8, status→#10, confidence→#7); no placeholders remain.

## Decisions (rationale in decisions.md)

- Schema lives *in* the orchestrator skill, not a standalone doc (self-containment).
- `operationalize` declares + defaults the estimand to ITT; stage skills own artifact sections.
- `appraise-study`: RoB-2 mechanism, own questions/algorithm, `No Information`→gaps, non-RCT gate.
- `lit-review`: grade the root claim in step 1, defer per-crux grading to step 2; GRADE-based not GRADE-badged.
- `find-cruxes`: qualitative VOI = leverage × liveness; per-crux grading here.
- `find-pending-evidence`: tool-detection implemented here (#13 generalizes); relevance gate; no fabricated IDs.
- `write-update-rules`: mandatory inconclusive branch; qualitative magnitude; data-driven recheck.
- Orchestrator: contract trace clean; three conductor-only threading duties.

## Issues

- **Closed:** #12, #6, #8, #7, #9, #10, #11, #5 (the whole core).
- **Opened:** #22 (de-egg examples).
- **Updated:** #19 (two pre-registration comments — predictions + revised run design).

## Note on process

Mid-session the user challenged whether breadth-first-then-test was right; established that the honest test is clean-room-only, so in-context validation is impossible and finishing the core in-context (then testing fresh) is the correct partition — with paper-tracing contracts as the in-context integration check. The user further sharpened the test design (honesty-run framing, framing-matched control, non-egg validation). Good adversarial-collaboration session.
