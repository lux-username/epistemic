# epistemic

A prototype tool that, for an open question, identifies the *crux* — the sub-question that most changes the answer — and surfaces promising research that may imminently resolve it. Built for the Future of Life Foundation "Epistemic Case Study Competition."

<!-- play/demo link here if any -->

## Getting back up to speed

1. This file
2. `CLAUDE.md` — how we work here
3. `STATUS.md` — where things stand right now (check the stamp date at the top)
4. `gh issue list --state open` — what's open; the active Milestone is the roadmap

Deeper: `spec.md` (design of record) · `decisions.md` (why X over Y) · `MAP.md` (annotated repo layout) · `journal/` (session-by-session history; each entry points to its raw transcript in gitignored `sessions/`).
